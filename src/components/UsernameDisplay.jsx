//this component should render the current username

const UsernameDisplay = () => {
    // 👇 ask for the current username and then show it 


    return (
        <div>
            {'put here the username instead of this text'}
        </div>
    );
};

export default UsernameDisplay;